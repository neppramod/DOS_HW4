#ifndef DFS_H
#define DFS_H

/*
class Dfs
{
public:
    dfs();
    bool processed[MAX_NEIGHBORS_NODE_SIZE];   // which node have been processed
    bool discovered [MAX_NEIGHBORS_NODE_SIZE];  //which node have been found
    int parent [MAX_NEIGHBORS_NODE_SIZE];       // discovery relation
    void initialize_search();                   // initialize the search
    void find_path();                           // find the path
    void dfs_search(char *filename, int node_id);  //search recursively for the file

};

*/
#endif // DFS_H
